import React from 'react';
import Layout from '../Layout';

const LayoutSpinner = () => {
    return(
        <Layout>
            <div className="layoutSpinner">
                loading...
            </div>
        </Layout>
    )
}

export default LayoutSpinner;